initialization
initializeOfWidth: aWidth height: aHeight tileWidth: aTileWidth tileHeight: aTileHeight

	width := aWidth.
	height := aHeight.
	tileWidth := aTileWidth.
	tileHeight := aTileHeight.
	cells := Matrix rows: height columns: width